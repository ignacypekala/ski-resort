package io.github.ignacypekala;

public class SkierTracked extends Skier {

    public SkierTracked(
        Vertex startPoint,
        int proficiency,
        int identifier,
        int startTime,
        double spontaneity
    ) {
        super(
            startPoint,
            proficiency,
            identifier,
            startTime,
            spontaneity
        );
    }
}
